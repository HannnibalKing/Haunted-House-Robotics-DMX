{
  "name": "show-orchestrator",
  "version": "0.1.0",
  "type": "module",
  "description": "Real-time control bridge between haunted_sim scenarios and physical hardware",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "dev": "ts-node --esm src/index.ts",
    "lint": "eslint . --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "mqtt": "^5.7.0",
    "pino": "^9.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@typescript-eslint/eslint-plugin": "^7.12.0",
    "@typescript-eslint/parser": "^7.12.0",
    "eslint": "^8.57.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
